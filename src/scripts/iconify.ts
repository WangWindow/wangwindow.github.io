import "iconify-icon";
import { addCollection } from "@iconify/iconify";
import tablerIcons from "@iconify-json/tabler/icons.json";

// Register icon set locally so icons render immediately without network fetch.
addCollection(tablerIcons);
